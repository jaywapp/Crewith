# PR CI 실패 수정 설계

기존 정규식은 사용자 피드백의 제어문자를 의도적으로 제거한다. 동일 처리와 할당 특성을 유지하고 해당 한 줄에만 이유를 명시한 no-control-regex 예외 주석을 단다. 전체 제어문자 경계와 Unicode 보존 회귀 테스트를 추가한다. 워크플로와 전체 린트 규칙은 유지한다.

검증: API 전체 테스트·타입 검사·동일 dist ESLint 명령 및 새 PR HEAD API CI 성공 확인.
